{
  "name": "@fmmenchi/signing",
  "version": "0.0.1",
  "description": "Framework-agnostic HMAC signing — sign a value into an opaque, tamper-proof, optionally-expiring token and verify it back. Web Crypto only (Node / edge / worker), no dependencies.",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fmmenchi/shared-platform.git",
    "directory": "packages/server/signing"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "files": [
    "dist",
    "!**/*.tsbuildinfo"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    "./package.json": "./package.json",
    ".": {
      "@fmmenchi/source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "nx": {
    "tags": [
      "scope:server",
      "type:util",
      "doc:server"
    ],
    "targets": {
      "sbom": {
        "executor": "@fmmenchi/nx-trivy:sbom",
        "cache": false,
        "configurations": {
          "docker": {
            "runner": "docker"
          }
        }
      }
    }
  },
  "dependencies": {
    "tslib": "^2.3.0"
  }
}
