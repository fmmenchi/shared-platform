/** The generated manifest of doc-enabled projects, written into the docs app root. */
export const configFileName = 'nx-doc-projects.json';

/** The per-project folder a project puts its documentation in. */
export const docsFolder = 'docs';
