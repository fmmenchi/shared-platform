{
  "executors": {
    "announce-release": {
      "implementation": "./dist/executors/announce-release/executor",
      "schema": "./dist/executors/announce-release/schema.json",
      "description": "Announce a freshly cut release to Slack, with a changelog."
    },
    "announce-error": {
      "implementation": "./dist/executors/announce-error/executor",
      "schema": "./dist/executors/announce-error/schema.json",
      "description": "Announce an error/alert to Slack with a link back to the run."
    }
  }
}
