{
  "$schema": "https://json-schema.org/schema",
  "$id": "NxTrivySbom",
  "title": "Add the sbom target to a project",
  "description": "Adds an `sbom` target (CycloneDX bill of materials for the project's dependency closure) to a project, plus a `docker` configuration that runs it via the aquasec/trivy image.",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The project that should publish a bill of materials.",
      "$default": { "$source": "argv", "index": 0 },
      "x-dropdown": "projects",
      "x-prompt": "Which project should publish an SBOM?"
    },
    "skipFormat": {
      "type": "boolean",
      "description": "Skip formatting the files touched by this generator.",
      "default": false
    }
  },
  "required": ["project"]
}
