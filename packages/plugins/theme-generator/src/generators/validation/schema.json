{
  "$schema": "https://json-schema.org/schema",
  "$id": "ThemeValidation",
  "title": "Wire theme validation on a project",
  "description": "Adds (or updates) a `validate-themes` target running the @fmmenchi/nx-theme-generator:validate executor, registering the given theme CSS files.",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The project that owns the themes.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Which project owns the themes?"
    },
    "themes": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Workspace-relative theme CSS paths to register.",
      "default": []
    }
  },
  "required": ["project"]
}
