import type { Meta, StoryObj } from '@storybook/react-vite';
import { <%= className %> } from './<%= fileName %>.component.js';

const meta: Meta<typeof <%= className %>> = {
  // TODO: file it under its GROUP — `Components/<Group>/<%= className %>`, where
  // <Group> is the row of the Shipped table in `docs/roadmap.md` that names this
  // component. The sidebar and that page are one taxonomy, and
  // `src/test/roadmap.test.ts` fails until they agree: an ungrouped title like
  // the one below is how `Breadcrumb`, `Progress` and `Separator` each ended up
  // loose at the top of the sidebar.
  title: 'Components/<%= className %>',
  component: <%= className %>,
  args: { children: '<%= className %>' },
  // The Props table is CURATED here (react-docgen can't derive it) — declare
  // every public prop with type summary, default and description.
  argTypes: {
    variant: {
      control: 'inline-radio',
      options: ['primary'],
      description: 'TODO — maps to a token role/treatment.',
      table: { type: { summary: "'primary'" }, defaultValue: { summary: "'primary'" } },
    },
  },
};
export default meta;

type Story = StoryObj<typeof <%= className %>>;

/** All variants at a glance. */
export const Gallery: Story = {
  render: () => (
    <div style={{ display: 'flex', gap: 'var(--fm-space-inline-s)' }}>
      <<%= className %> variant="primary">Primary</<%= className %>>
    </div>
  ),
};

export const Primary: Story = { args: { variant: 'primary' } };
