import type { ButtonLikeProps } from '../../primitives/button-like.types.js';

/**
 * The same props as `PopoverTrigger`: a `Button`'s, plus an `as` for anything
 * that accepts them and ends in a `<button>`.
 */
export type PopoverCloseProps = ButtonLikeProps;
