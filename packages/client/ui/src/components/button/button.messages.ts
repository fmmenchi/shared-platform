import { defineMessages } from '../../i18n/messages.js';

/** Button's own micro-copy, colocated with the component. */
export const buttonMessages = defineMessages('button', {
  en: { loading: 'Loading' },
  it: { loading: 'Caricamento' },
  ar: { loading: 'جارٍ التحميل' },
});
