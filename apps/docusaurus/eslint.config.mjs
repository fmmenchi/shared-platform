// Docs site: only config files live here — no lint rules needed. This config
// exists to keep the generated output out of any workspace-level lint run.
export default [
  {
    ignores: ['build', '.docusaurus'],
  },
];
